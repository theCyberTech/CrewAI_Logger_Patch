from setuptools import setup, find_packages

setup(
    name='crewai-logger_patch',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'crewai',
        # other dependencies
    ],
)
